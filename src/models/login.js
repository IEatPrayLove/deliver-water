import {observable, action} from 'mobx';

class Login {
//   @observable hasLogin = true;

  constructor(rootStore) {
    this.rootStore = rootStore;
  }

//   @action setHasLogin = (bol) => {
//     this.hasLogin = bol;
//   };
}

export default Login;
